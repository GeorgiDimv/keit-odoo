# Copy to .env (cp .env.example .env). Local dev only — do not reuse in prod.

COMPOSE_PROJECT_NAME=odoo18

# PostgreSQL
POSTGRES_USER=odoo
POSTGRES_PASSWORD=odoo
POSTGRES_DB=postgres

# Odoo
ODOO_ADMIN_PASSWD=admin
ODOO_PORT=8069
ODOO_LONGPOLLING_PORT=8072
